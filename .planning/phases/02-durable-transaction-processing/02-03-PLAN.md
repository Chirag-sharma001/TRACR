---
phase: 02-durable-transaction-processing
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/src/observability/DurabilityHealthMetrics.js
  - backend/src/observability/DurabilityHealthMetrics.test.js
  - backend/src/routes/admin.js
  - backend/src/routes/AdminDurabilityTelemetry.contract.test.js
autonomous: true
requirements:
  - DET-02
must_haves:
  truths:
    - "Operations can retrieve backlog/recovery durability health signals from an admin endpoint (D-09)."
    - "Health output reflects failed backlog count, replay throughput, and oldest outstanding failure age."
    - "Telemetry windows are bounded so operational polling cannot exhaust the service."
  artifacts:
    - path: "backend/src/observability/DurabilityHealthMetrics.js"
      provides: "Durability and replay backlog aggregation service"
    - path: "backend/src/routes/admin.js"
      provides: "Admin telemetry endpoint for processing durability health"
    - path: "backend/src/routes/AdminDurabilityTelemetry.contract.test.js"
      provides: "Contract tests for role-gated durability telemetry output"
  key_links:
    - from: "backend/src/routes/admin.js"
      to: "backend/src/observability/DurabilityHealthMetrics.js"
      via: "GET telemetry route delegation"
    - from: "backend/src/observability/DurabilityHealthMetrics.js"
      to: "backend/src/models/ProcessingFailure.js"
      via: "failed backlog and recovery lag aggregation"
---

<objective>
Provide operator-facing durability health telemetry so teams can monitor backlog and replay recovery posture (D-09).

Purpose: Add operational visibility needed to confirm durable catch-up and replay control health.
Output: Durability metrics service, admin telemetry route, and contract tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-durable-transaction-processing/02-CONTEXT.md
@.planning/phases/02-durable-transaction-processing/02-RESEARCH.md
@.planning/phases/01-governance-contracts-and-observability/01-03-SUMMARY.md
@backend/src/observability/DetectionQualityMetrics.js
@backend/src/routes/admin.js

<interfaces>
From backend/src/observability/DetectionQualityMetrics.js:
```javascript
class DetectionQualityMetrics {
  async getDetectionQualityTelemetry({ day_window_days, week_window_weeks } = {}) {}
}
```

From backend/src/routes/admin.js:
```javascript
router.get("/telemetry/detection-quality", async (req, res) => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define durability telemetry contract and failing tests</name>
  <files>backend/src/observability/DurabilityHealthMetrics.js, backend/src/observability/DurabilityHealthMetrics.test.js, backend/src/routes/AdminDurabilityTelemetry.contract.test.js</files>
  <behavior>
    - Telemetry response includes backlog and replay health keys in snake_case.
    - Window bounds are enforced for daily/weekly-like controls.
    - Empty backlog returns stable zeroed metrics shape.
  </behavior>
  <action>Create RED tests for `DurabilityHealthMetrics` and admin route contract before implementation. Define response schema keys: `failed_backlog_total`, `failed_oldest_age_seconds`, `replayed_last_24h`, `failed_last_24h`, and `window_bounds` so D-09 is testable and consistent with existing telemetry conventions.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/observability/DurabilityHealthMetrics.test.js src/routes/AdminDurabilityTelemetry.contract.test.js</automated>
  </verify>
  <done>Failing tests clearly define durability telemetry behavior and response contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement durability health aggregation service and admin endpoint</name>
  <files>backend/src/observability/DurabilityHealthMetrics.js, backend/src/routes/admin.js, backend/src/observability/DurabilityHealthMetrics.test.js, backend/src/routes/AdminDurabilityTelemetry.contract.test.js</files>
  <behavior>
    - Admin endpoint returns bounded, deterministic durability health metrics.
    - Non-admin callers are denied by existing admin middleware chain.
    - Aggregations are resilient when collections are empty or partially populated.
  </behavior>
  <action>Implement `DurabilityHealthMetrics.getDurabilityHealth` using `ProcessingFailure` and `ProcessingLedger` aggregations with bounded window inputs. Add `GET /api/admin/telemetry/processing-durability` in `admin.js` following existing detection telemetry style and keep response fields snake_case. Ensure endpoint uses existing ADMIN middleware and returns stable schema on errors with explicit `telemetry_failed` handling.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/observability/DurabilityHealthMetrics.test.js src/routes/AdminDurabilityTelemetry.contract.test.js</automated>
  </verify>
  <done>Operations can query durability health metrics safely and consistently via admin telemetry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client -> telemetry route | Untrusted query parameters can attempt expensive aggregation ranges |
| telemetry service -> ledger/failure collections | Aggregation boundary can leak sensitive operational detail if not role-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | D | Telemetry query parameters | mitigate | Clamp query windows and enforce hard max bounds before aggregation execution |
| T-02-10 | I | Durability telemetry route | mitigate | Keep endpoint under existing ADMIN middleware and expose only aggregate metrics (no raw transaction payloads) |
| T-02-11 | T | Metrics schema stability | mitigate | Contract tests enforce fixed snake_case response keys and bounded numeric outputs |
| T-02-12 | R | Operational monitoring decisions | accept | Aggregated telemetry is advisory; operational actions remain validated by replay endpoints and audit logs |
</threat_model>

<verification>
- `cd backend && npx jest --runInBand src/observability/DurabilityHealthMetrics.test.js src/routes/AdminDurabilityTelemetry.contract.test.js`
</verification>

<success_criteria>
- D-09 backlog/recovery health endpoint is available to operations/admin users.
- Durability metrics are bounded, stable, and contract-tested.
- Telemetry aligns with DET-02 replay/catch-up observability expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/02-durable-transaction-processing/02-03-SUMMARY.md`
</output>
