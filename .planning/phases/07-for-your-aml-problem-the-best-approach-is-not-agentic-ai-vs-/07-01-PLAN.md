---
phase: 07-for-your-aml-problem-the-best-approach-is-not-agentic-ai-vs-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/policy/HybridBoundaryPolicy.js
  - backend/src/policy/HybridBoundaryPolicy.test.js
  - backend/src/detection/DetectionOrchestrator.js
  - backend/src/detection/DetectionOrchestrator.test.js
  - backend/src/detection/DetectionOrchestrator.property.test.js
autonomous: true
requirements:
  - PH7-HYBRID-BOUNDARY
user_setup: []
must_haves:
  truths:
    - "Graph-pattern assertions remain deterministic and DFS-confirmed"
    - "AI anomaly hints are stored as candidates until deterministic confirmation"
    - "Graph-pattern alerts always carry deterministic evidence artifacts"
  artifacts:
    - path: "backend/src/policy/HybridBoundaryPolicy.js"
      provides: "Boundary policy contract and deterministic confirmation helpers"
      exports: ["confirmDeterministicGraphHit", "buildDeterministicEvidence", "markAiCandidate"]
    - path: "backend/src/detection/DetectionOrchestrator.js"
      provides: "Deterministic truth gate before alert emission"
      contains: "hybrid_boundary"
  key_links:
    - from: "backend/src/detection/DetectionOrchestrator.js"
      to: "backend/src/policy/HybridBoundaryPolicy.js"
      via: "policy contract invocation during analyze/onTransactionSaved"
      pattern: "confirmDeterministicGraphHit|buildDeterministicEvidence"
---

<objective>
Lock the deterministic truth boundary for graph-pattern detection and prevent AI suggestions from being treated as confirmed typology hits.

Purpose: Implement D-01, D-02, and D-03 as enforceable runtime contracts, not implicit behavior.
Output: A reusable hybrid boundary policy module, orchestrator enforcement, and regression tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-for-your-aml-problem-the-best-approach-is-not-agentic-ai-vs-/07-CONTEXT.md
@.planning/phases/07-for-your-aml-problem-the-best-approach-is-not-agentic-ai-vs-/07-RESEARCH.md
@backend/src/detection/DetectionOrchestrator.js
@backend/src/detection/CycleDetector.js
@backend/src/detection/GraphManager.js

<interfaces>
From backend/src/detection/DetectionOrchestrator.js:
```javascript
class DetectionOrchestrator {
  async analyze(tx) {}
  async onTransactionSaved(tx) {}
}
module.exports = DetectionOrchestrator;
```

From backend/src/detection/CycleDetector.js:
```javascript
class CycleDetector {
  detectCycles(graph, newEdge, maxLength, timeWindowHours) {}
}
module.exports = CycleDetector;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create hybrid boundary policy contract (D-01, D-02, D-03)</name>
  <read_first>
    .planning/phases/07-for-your-aml-problem-the-best-approach-is-not-agentic-ai-vs-/07-CONTEXT.md
    backend/src/detection/CycleDetector.js
    backend/src/detection/GraphManager.js
  </read_first>
  <files>backend/src/policy/HybridBoundaryPolicy.js, backend/src/policy/HybridBoundaryPolicy.test.js</files>
  <behavior>
    - Test 1: DFS-confirmed cycle evidence returns confirmed graph-pattern verdict.
    - Test 2: AI-only candidate never returns confirmed graph-pattern verdict.
    - Test 3: Deterministic evidence artifact always contains path edges, involved accounts, and bounded window metadata.
  </behavior>
  <action>Implement a policy module that exposes deterministic confirmation and evidence-packet helpers. The module must tag AI inputs as candidate-only unless deterministic cycle/path evidence exists per D-02 and must normalize evidence artifacts per D-03.</action>
  <verify>
    <automated>cd backend && npx jest src/policy/HybridBoundaryPolicy.test.js --runInBand</automated>
  </verify>
  <acceptance_criteria>Hybrid policy helpers exist, are unit-tested, and explicitly enforce candidate-vs-confirmed graph-pattern states.</acceptance_criteria>
  <done>Policy contract passes tests and exports reusable helpers for orchestrator integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce deterministic truth gate in detection orchestration (D-01, D-02)</name>
  <read_first>
    backend/src/detection/DetectionOrchestrator.js
    backend/src/policy/HybridBoundaryPolicy.js
    .planning/phases/07-for-your-aml-problem-the-best-approach-is-not-agentic-ai-vs-/07-CONTEXT.md
  </read_first>
  <files>backend/src/detection/DetectionOrchestrator.js, backend/src/detection/DetectionOrchestrator.test.js</files>
  <behavior>
    - Test 1: Confirmed DFS graph evidence emits graph-pattern-capable detection result.
    - Test 2: AI candidate without DFS evidence is marked unconfirmed and does not emit confirmed graph typology claim.
  </behavior>
  <action>Wire the policy module into `analyze` and `onTransactionSaved`, adding explicit boundary metadata to detection output. Ensure alert flow can distinguish deterministic confirmations from AI suggestions and never treats suggestions as confirmed typology hits.</action>
  <verify>
    <automated>cd backend && npx jest src/detection/DetectionOrchestrator.test.js --runInBand</automated>
  </verify>
  <acceptance_criteria>Orchestrator output now carries deterministic confirmation state, and AI-only suggestions remain advisory candidates.</acceptance_criteria>
  <done>Runtime path enforces deterministic confirmation before graph-pattern truth is asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add invariant/property tests for deterministic evidence requirements (D-03)</name>
  <read_first>
    backend/src/detection/DetectionOrchestrator.property.test.js
    backend/src/policy/HybridBoundaryPolicy.test.js
    .planning/phases/07-for-your-aml-problem-the-best-approach-is-not-agentic-ai-vs-/07-RESEARCH.md
  </read_first>
  <files>backend/src/detection/DetectionOrchestrator.property.test.js</files>
  <behavior>
    - Property: Any confirmed graph-pattern result includes non-empty involved_accounts and transaction_sequence.
    - Property: Confirmed result window metadata is present and bounded.
  </behavior>
  <action>Extend property-based tests to lock D-03 invariants so future refactors cannot emit confirmed graph-pattern alerts without deterministic evidence artifacts.</action>
  <verify>
    <automated>cd backend && npx jest src/detection/DetectionOrchestrator.property.test.js --runInBand</automated>
  </verify>
  <acceptance_criteria>Property suite fails when deterministic evidence fields are missing and passes with valid evidence packets.</acceptance_criteria>
  <done>Deterministic evidence contract is protected by automated invariants.</done>
</task>

</tasks>

<threat_model>
Blocking rule: Any threat marked HIGH must be mitigated in this plan before wave completion.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detector pipeline -> alert emission | untrusted AI candidate signals cross into regulated alert truth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | Detection boundary state | HIGH | mitigate-blocking | Enforce deterministic confirmation check in orchestrator before graph-pattern truth emission. |
| T-07-02 | Repudiation | Evidence packet completeness | MEDIUM | mitigate | Property tests require mandatory deterministic evidence fields for confirmed graph patterns. |
| T-07-03 | Denial of Service | Boundary policy branch logic | LOW | accept | Keep checks O(1) and reuse existing detector outputs; no new heavy traversal in boundary gate. |
</threat_model>

<verification>
Run:
- `cd backend && npx jest src/policy/HybridBoundaryPolicy.test.js --runInBand`
- `cd backend && npx jest src/detection/DetectionOrchestrator.test.js --runInBand`
- `cd backend && npx jest src/detection/DetectionOrchestrator.property.test.js --runInBand`
</verification>

<success_criteria>
- D-01 and D-02 are enforced by code and tests: AI cannot mark graph-pattern truth without DFS confirmation.
- D-03 is enforced by invariant tests: confirmed graph-pattern outputs always include deterministic evidence artifacts.
- No HIGH-severity threat remains unmitigated for this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/07-for-your-aml-problem-the-best-approach-is-not-agentic-ai-vs-/07-01-SUMMARY.md`
</output>
