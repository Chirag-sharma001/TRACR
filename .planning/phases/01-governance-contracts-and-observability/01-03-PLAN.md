---
phase: 01-governance-contracts-and-observability
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - backend/src/models/Alert.js
  - backend/src/scoring/RiskScorer.js
  - backend/src/observability/DetectionQualityMetrics.js
  - backend/src/routes/admin.js
  - backend/src/observability/DetectionQualityMetrics.test.js
  - backend/src/routes/AdminTelemetry.contract.test.js
autonomous: true
requirements:
  - DET-03
must_haves:
  truths:
    - "Compliance manager can retrieve quality telemetry segmented by detector type and risk tier."
    - "Telemetry includes config version lineage for before/after governance comparisons."
    - "Telemetry supports daily and weekly windows from one stable API contract."
  artifacts:
    - path: "backend/src/observability/DetectionQualityMetrics.js"
      provides: "Aggregation contract for detector/risk/config-version quality telemetry"
    - path: "backend/src/routes/admin.js"
      provides: "Admin endpoint returning daily and weekly telemetry views"
    - path: "backend/src/routes/AdminTelemetry.contract.test.js"
      provides: "Contract test for DET-03 response shape and segmentation"
  key_links:
    - from: "backend/src/scoring/RiskScorer.js"
      to: "backend/src/models/Alert.js"
      via: "Persist config_version_id on alert creation"
    - from: "backend/src/observability/DetectionQualityMetrics.js"
      to: "backend/src/routes/admin.js"
      via: "Admin telemetry endpoint delegates to aggregation service"
---

<objective>
Deliver DET-03 observability contracts with detector/risk/version segmentation and daily+weekly windows (D-05, D-06, D-07, D-08).

Purpose: Give compliance governance reliable drift/calibration trend visibility tied to published config versions.
Output: Telemetry service, lineage-aware alert persistence, admin telemetry endpoint, and contract tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-governance-contracts-and-observability/01-CONTEXT.md
@.planning/phases/01-governance-contracts-and-observability/01-RESEARCH.md
@.planning/phases/01-governance-contracts-and-observability/01-02-PLAN.md
@backend/src/realtime/SocketGateway.js
@backend/src/scoring/RiskScorer.js
@backend/src/models/Alert.js

<interfaces>
From backend/src/scoring/RiskScorer.js:
```javascript
async compute(detectionResult, geolocation) { ... return payload; }
```

From backend/src/models/Alert.js:
```javascript
pattern_type: "CIRCULAR_TRADING" | "SMURFING" | "BEHAVIORAL_ANOMALY"
risk_tier: "LOW" | "MEDIUM" | "HIGH"
created_at: Date
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add config lineage fields to alert persistence for governance comparison</name>
  <files>backend/src/models/Alert.js, backend/src/scoring/RiskScorer.js, backend/src/observability/DetectionQualityMetrics.test.js</files>
  <behavior>
    - New alerts persist `config_version_id` and `published_change_id` from active threshold lineage.
    - Existing alert creation paths remain backward compatible when lineage is missing.
  </behavior>
  <action>Extend `Alert` schema with lineage fields and update `RiskScorer` to read active lineage values from threshold config/service context when writing alerts. Preserve existing output contract fields and avoid breaking current detection scoring behavior.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/scoring/RiskScorer.property.test.js src/observability/DetectionQualityMetrics.test.js</automated>
  </verify>
  <done>Alert records carry version lineage needed for governance comparisons while existing scorer behavior remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement detector and risk-tier segmented daily/weekly telemetry service</name>
  <files>backend/src/observability/DetectionQualityMetrics.js, backend/src/observability/DetectionQualityMetrics.test.js</files>
  <behavior>
    - Aggregation returns detector segments for `cycle`, `smurfing`, `behavioral` (D-05).
    - Aggregation includes risk-tier segmentation (D-06).
    - Aggregation includes lineage groupings by `config_version_id` (D-07).
    - Aggregation supports daily and weekly windows in one API call (D-08).
  </behavior>
  <action>Create observability service using Mongo aggregation with explicit UTC day/week bucket logic and stable response schema; normalize detector enum values to the required contract keys without introducing UI scope.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/observability/DetectionQualityMetrics.test.js</automated>
  </verify>
  <done>Service returns deterministic segmented daily+weekly telemetry contract suitable for compliance governance review.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose DET-03 telemetry endpoint under admin routes with contract tests</name>
  <files>backend/src/routes/admin.js, backend/src/routes/AdminTelemetry.contract.test.js</files>
  <behavior>
    - Admin endpoint responds with detector/risk/version segmented windows.
    - Unauthorized/non-admin requests are denied through existing middleware chain.
  </behavior>
  <action>Add `GET /telemetry/detection-quality` in `createAdminRoutes`, delegating to `DetectionQualityMetrics` service and returning daily/weekly buckets in snake_case fields. Keep response backend-focused only (D-12) and avoid what-if simulation scope.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/routes/AdminTelemetry.contract.test.js</automated>
  </verify>
  <done>Compliance can retrieve DET-03 telemetry through admin API with a stable, tested contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alert storage -> telemetry aggregation | Stored lineage and detector values influence governance decisions |
| admin telemetry API -> compliance consumers | Read APIs can leak sensitive operational patterns if access controls fail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | T | telemetry aggregation pipeline | mitigate | Enforce strict grouping keys and schema-level normalization for detector/risk/version fields |
| T-01-10 | I | `GET /telemetry/detection-quality` | mitigate | Keep ADMIN middleware chain and return only governance aggregates, not raw PII |
| T-01-11 | R | lineage-aware telemetry | mitigate | Include `config_version_id` and `published_change_id` in aggregate output for traceability |
| T-01-12 | D | weekly/day aggregation query load | accept | Bound windows and index grouping fields; phase scope does not include external analytics store |
</threat_model>

<verification>
Run telemetry contract and aggregation tests:
- `cd backend && npx jest --runInBand src/observability/DetectionQualityMetrics.test.js src/routes/AdminTelemetry.contract.test.js`
</verification>

<success_criteria>
- DET-03 telemetry contract exposes detector/risk/version segmented daily and weekly views.
- Lineage fields allow before/after config governance comparisons.
- Admin API contract is test-covered and remains within backend-only phase surface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-governance-contracts-and-observability/01-03-SUMMARY.md`
</output>
