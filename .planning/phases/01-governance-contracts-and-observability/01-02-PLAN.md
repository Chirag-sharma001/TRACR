---
phase: 01-governance-contracts-and-observability
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/src/routes/admin.js
  - backend/src/audit/AuditLogger.js
  - backend/src/routes/AdminGovernance.lifecycle.test.js
  - backend/src/routes/AdminGovernance.property.test.js
  - backend/src/governance/ConfigGovernanceService.js
autonomous: true
requirements:
  - GOV-03
must_haves:
  truths:
    - "Admin submits config change requests through explicit governance endpoints, not direct active mutation."
    - "Rollback can be executed immediately by authorized actor but always requires reason and linked original change id."
    - "Every submit/approve/activate/rollback action writes immutable audit records with actor and reason context."
  artifacts:
    - path: "backend/src/routes/admin.js"
      provides: "Governance lifecycle API endpoints and rollback contract"
    - path: "backend/src/audit/AuditLogger.js"
      provides: "Immutable event logging support for governance actions"
    - path: "backend/src/routes/AdminGovernance.lifecycle.test.js"
      provides: "Route-level lifecycle tests for submit/approve/activate/rollback"
  key_links:
    - from: "backend/src/routes/admin.js"
      to: "backend/src/governance/ConfigGovernanceService.js"
      via: "Route handlers delegate lifecycle transitions"
    - from: "backend/src/routes/admin.js"
      to: "backend/src/audit/AuditLogger.js"
      via: "Governance actions emit immutable audit events"
---

<objective>
Wire governance contracts into admin APIs with immutable rollback-audit semantics (D-09, D-10, D-11) while preserving backend-first delivery (D-12).

Purpose: Convert governance state machine into executable API workflow for admins.
Output: Admin lifecycle endpoints, rollback semantics, and route-level tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-governance-contracts-and-observability/01-CONTEXT.md
@.planning/phases/01-governance-contracts-and-observability/01-RESEARCH.md
@.planning/phases/01-governance-contracts-and-observability/01-01-PLAN.md
@backend/src/routes/admin.js
@backend/src/audit/AuditLogger.js

<interfaces>
From backend/src/audit/AuditLogger.js:
```javascript
async log({ userId, userRole, actionType, resourceType, resourceId, outcome, metadata = {}, ipAddress = null })
```

From backend/src/routes/admin.js:
```javascript
router.put("/config", async (req, res) => { ... });
router.get("/audit", async (req, res) => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace direct-write admin config flow with governance lifecycle endpoints</name>
  <files>backend/src/routes/admin.js, backend/src/routes/AdminGovernance.lifecycle.test.js</files>
  <behavior>
    - `POST /config/changes` creates draft request with required metadata (D-03).
    - `POST /config/changes/:id/approve` enforces requester != approver (D-02).
    - `POST /config/changes/:id/activate` rejects unapproved requests (D-04).
  </behavior>
  <action>Extend `createAdminRoutes` with submit/approve/activate endpoints that call `ConfigGovernanceService` and keep JWT+ADMIN middleware chain. Preserve existing `/config` read route, but route mutating publish operations through lifecycle endpoints instead of immediate direct writes.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/routes/AdminGovernance.lifecycle.test.js</automated>
  </verify>
  <done>Admin config publication follows draft -> approved -> active lifecycle and no route path allows self-approval or pre-approval activation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement rollback endpoint with mandatory provenance and immutable audit capture</name>
  <files>backend/src/routes/admin.js, backend/src/audit/AuditLogger.js, backend/src/routes/AdminGovernance.property.test.js</files>
  <behavior>
    - Rollback endpoint requires `rollback_reason` and `original_change_id` (D-09).
    - Successful rollback restores prior approved config snapshot without manual patching (D-11).
    - Submit/approve/activate/rollback events are append-only audit records (D-10).
  </behavior>
  <action>Add `POST /config/changes/:id/rollback` route and service invocation that restores previous approved config snapshot, links rollback to origin change id, and writes `CONFIG_SUBMIT`, `CONFIG_APPROVE`, `CONFIG_ACTIVATE`, and `CONFIG_ROLLBACK` events through `AuditLogger` with immutable metadata payloads.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/routes/AdminGovernance.property.test.js src/audit/AuditLogger.property.test.js</automated>
  </verify>
  <done>Rollback path is policy-safe, provenance-linked, and end-to-end auditable with immutable event records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client -> admin routes | User-controlled payloads and ids enter privileged config workflow routes |
| admin routes -> audit log storage | Governance evidence must remain append-only and tamper resistant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | E | `/config/changes/:id/approve` | mitigate | Require ADMIN role and service-level requester/approver inequality check |
| T-01-06 | T | rollback payload | mitigate | Enforce required `rollback_reason` and `original_change_id` schema validation before execution |
| T-01-07 | R | governance audit records | mitigate | Emit append-only action events with actor id, role, reason, and linked change id |
| T-01-08 | I | audit retrieval endpoint | transfer | Existing role controls and data minimization remain in admin middleware scope |
</threat_model>

<verification>
Run route and audit tests:
- `cd backend && npx jest --runInBand src/routes/AdminGovernance.lifecycle.test.js src/routes/AdminGovernance.property.test.js src/audit/AuditLogger.property.test.js`
</verification>

<success_criteria>
- GOV-03 workflow is executable through admin APIs with approval gating.
- Rollback is immediate-for-authorized users, provenance-linked, and audit-immutable.
- Route/property tests prove no bypass for self-approval and activation gating.
</success_criteria>

<output>
After completion, create `.planning/phases/01-governance-contracts-and-observability/01-02-SUMMARY.md`
</output>
