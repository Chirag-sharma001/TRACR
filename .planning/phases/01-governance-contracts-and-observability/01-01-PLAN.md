---
phase: 01-governance-contracts-and-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/ConfigChangeRequest.js
  - backend/src/models/SystemConfig.js
  - backend/src/models/SystemConfig.test.js
  - backend/src/governance/ConfigGovernanceService.js
  - backend/src/governance/ConfigGovernance.property.test.js
autonomous: true
requirements:
  - GOV-03
must_haves:
  truths:
    - "Admin can create a config change request that stays non-active until approved."
    - "Requester identity and structured change metadata are always recorded on creation."
    - "Requester cannot self-approve and activation requires approved state."
  artifacts:
    - path: "backend/src/models/ConfigChangeRequest.js"
      provides: "Draft/approved/active/rolled_back lifecycle persistence contract"
    - path: "backend/src/governance/ConfigGovernanceService.js"
      provides: "Two-person control and approval-gate state transition logic"
    - path: "backend/src/governance/ConfigGovernance.property.test.js"
      provides: "Invariant tests for self-approval and invalid transition prevention"
  key_links:
    - from: "backend/src/governance/ConfigGovernanceService.js"
      to: "backend/src/models/ConfigChangeRequest.js"
      via: "Submit/approve/activate transition writes"
    - from: "backend/src/governance/ConfigGovernanceService.js"
      to: "backend/src/models/SystemConfig.js"
      via: "Apply approved snapshot to persisted config"
---

<objective>
Define governance contracts that enforce two-person approval and approval-gated activation for config changes (D-01, D-02, D-03, D-04).

Purpose: Establish immutable, policy-safe config lifecycle primitives before route wiring.
Output: New governance model/service with transition invariants and property tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-governance-contracts-and-observability/01-CONTEXT.md
@.planning/phases/01-governance-contracts-and-observability/01-RESEARCH.md
@backend/src/routes/admin.js
@backend/src/models/SystemConfig.js

<interfaces>
From backend/src/routes/admin.js:
```javascript
function createAdminRoutes({ jwtMiddleware, auditLogger, thresholdConfig, systemConfigModel, auditLogModel } = {})
```

From backend/src/models/SystemConfig.js:
```javascript
module.exports = mongoose.model("SystemConfig", systemConfigSchema);
// Fields: config_key, value, valid_range, updated_by, updated_at
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add governance lifecycle persistence contracts</name>
  <files>backend/src/models/ConfigChangeRequest.js, backend/src/models/SystemConfig.js, backend/src/models/SystemConfig.test.js, backend/src/governance/ConfigGovernance.property.test.js</files>
  <action>Create `ConfigChangeRequest` schema with statuses `DRAFT`, `APPROVED`, `ACTIVE`, `ROLLED_BACK`, and required metadata fields per D-03 (`reason`, `requester_id`, `change_scope`, `detector_scope`, `risk_scope`). Extend `SystemConfig` with immutable lineage fields (`config_version_id`, `published_change_id`) needed later by telemetry lineage (D-07) while preserving existing keys and backward compatibility.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/models/SystemConfig.test.js src/governance/ConfigGovernance.property.test.js</automated>
  </verify>
  <done>Model contracts persist lifecycle state and structured metadata, and `SystemConfig` can store active lineage identifiers without breaking existing config reads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement governance state machine service with two-person control</name>
  <files>backend/src/governance/ConfigGovernanceService.js, backend/src/governance/ConfigGovernance.property.test.js</files>
  <behavior>
    - Submit action stores metadata and returns `DRAFT` request.
    - Approve action rejects when approver equals requester (D-02).
    - Activate action rejects unless request status is `APPROVED` (D-04).
    - Transition history captures actor and timestamps for downstream audit (D-10).
  </behavior>
  <action>Implement service methods `submitChange`, `approveChange`, and `activateApprovedChange` with strict guard clauses and deterministic transition ordering per D-01 through D-04. Keep all field names snake_case and CommonJS exports.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/governance/ConfigGovernance.property.test.js</automated>
  </verify>
  <done>Service guarantees no self-approval, no activation before approval, and successful activation links active config to a published change/version id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client -> governance service | Untrusted request payloads and actor identity claims cross into lifecycle logic |
| governance service -> config persistence | State transitions can tamper production behavior if guardrails fail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | E | ConfigGovernanceService approval flow | mitigate | Enforce requester != approver check in service layer and property tests |
| T-01-02 | T | ConfigChangeRequest metadata | mitigate | Validate required metadata fields at submit boundary before persistence |
| T-01-03 | R | Transition state history | mitigate | Append transition actor/timestamp entries and prohibit silent status mutation |
| T-01-04 | D | Activation path | accept | Single-request activation load is low; keep guard clauses and bounded writes |
</threat_model>

<verification>
Run model + property tests:
- `cd backend && npx jest --runInBand src/governance/ConfigGovernance.property.test.js`
</verification>

<success_criteria>
- GOV-03 contracts enforce two-person control and approval-before-activation.
- Structured metadata is mandatory on request creation.
- Lifecycle model and service are test-covered for invalid transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-governance-contracts-and-observability/01-01-SUMMARY.md`
</output>
