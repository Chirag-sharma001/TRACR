---
phase: 04-explainability-interface-and-evidence-replay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/alerts.js
  - backend/src/routes/AlertExplainability.contract.test.js
  - backend/src/routes/AlertRoutes.hybrid.property.test.js
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-03
must_haves:
  truths:
    - "Analyst and investigator can retrieve per-alert decomposition including cycle, smurfing, behavioral, and geographic components (EXP-01)."
    - "Alert explainability interface returns deterministic evidence path context with accounts, edges/sequence, and linked transaction identifiers (EXP-02)."
    - "Narrative rationale output is grounded in persisted packet summary/statements and tied to evidence references (EXP-03)."
  artifacts:
    - path: "backend/src/routes/alerts.js"
      provides: "Additive explainability interface route that returns decomposition + evidence + narrative contracts"
    - path: "backend/src/routes/AlertExplainability.contract.test.js"
      provides: "Route contract tests for explainability and evidence path payload guarantees"
    - path: "backend/src/routes/AlertRoutes.hybrid.property.test.js"
      provides: "Property-level compatibility checks preserving alert payload explainability shape"
  key_links:
    - from: "backend/src/routes/alerts.js"
      to: "backend/src/models/Alert.js"
      via: "Read persisted explainability_packet and deterministic evidence fields"
    - from: "backend/src/routes/alerts.js"
      to: "backend/src/scoring/RiskScorer.js"
      via: "Route contract aligns with scorer-produced explainability packet fields"
---

<objective>
Expose an investigator-facing explainability endpoint that delivers decomposition, evidence path context, and evidence-grounded narrative rationale from persisted alert artifacts.

Purpose: satisfy EXP-01/EXP-02/EXP-03 without introducing alternate scoring logic.
Output: additive alert explainability route + contract/property tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-explainability-interface-and-evidence-replay/04-CONTEXT.md
@backend/src/routes/alerts.js
@backend/src/scoring/RiskScorer.js
@backend/src/models/Alert.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED contract tests for explainability interface payload</name>
  <files>backend/src/routes/AlertExplainability.contract.test.js, backend/src/routes/AlertRoutes.hybrid.property.test.js</files>
  <behavior>
    - Explainability endpoint returns decomposition fields for cycle/smurfing/behavioral/geographic.
    - Evidence path payload includes ordered transaction sequence, linked accounts, and transaction IDs.
    - Narrative section includes summary and statements anchored to evidence refs.
  </behavior>
  <action>Create failing route-level tests for `GET /api/alerts/:id/explainability` and compatibility assertions to lock payload shape and evidence-grounded narrative behavior.
  </action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/routes/AlertExplainability.contract.test.js src/routes/AlertRoutes.hybrid.property.test.js</automated>
  </verify>
  <done>Tests fail initially and encode EXP-01/EXP-02/EXP-03 response contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explainability endpoint in alert routes</name>
  <files>backend/src/routes/alerts.js, backend/src/routes/AlertExplainability.contract.test.js</files>
  <behavior>
    - Endpoint is additive and does not break existing list/detail contracts.
    - Explainability payload is generated strictly from persisted alert fields/packet.
    - Missing alert IDs return existing `not_found` contract behavior.
  </behavior>
  <action>Implement `GET /:id/explainability` in alert routes using shared normalization helpers. Return decomposition, deterministic evidence path context, and narrative mapping/text with confidence and risk metadata.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/routes/AlertExplainability.contract.test.js src/routes/AlertRoutes.hybrid.property.test.js</automated>
  </verify>
  <done>Explainability route passes contract tests and preserves existing route behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alert API caller -> explainability response | Untrusted clients consume sensitive evidence context and may probe contract edge cases |
| persisted alert document -> API payload | Malformed/missing explainability fields could degrade analyst trust or produce inconsistent rationale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I | explainability payload construction | mitigate | Normalize decomposition/evidence/narrative defaults and enforce stable schema keys |
| T-04-02 | T | evidence path ordering | mitigate | Deterministic transaction sequence normalization with explicit ordering and fallback |
| T-04-03 | R | narrative rationale integrity | mitigate | Derive narrative output from persisted explainability packet only; avoid ad hoc generated claims |
| T-04-04 | D | route compatibility | mitigate | Additive endpoint only; maintain existing list/detail contracts and tests |
</threat_model>

<verification>
- `cd backend && npx jest --runInBand src/routes/AlertExplainability.contract.test.js src/routes/AlertRoutes.hybrid.property.test.js`
</verification>

<success_criteria>
- EXP-01 decomposition is accessible per alert via explainability interface.
- EXP-02 deterministic path evidence context is returned with ordered sequence.
- EXP-03 narrative rationale is evidence-grounded and contract-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/04-explainability-interface-and-evidence-replay/04-01-SUMMARY.md`
</output>
