---
phase: 04-explainability-interface-and-evidence-replay
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - backend/src/routes/alerts.js
  - backend/src/routes/AlertEvidenceReplay.contract.test.js
autonomous: true
requirements:
  - EXP-04
must_haves:
  truths:
    - "Investigator can replay suspicious movement through a timeline-ordered storyline view derived from alert evidence (EXP-04)."
    - "Replay payload exposes ordered steps, sequence edges, and involved account context needed to reconstruct progression."
    - "Replay endpoint remains deterministic for equivalent persisted evidence inputs."
  artifacts:
    - path: "backend/src/routes/alerts.js"
      provides: "Evidence replay route with timeline/storyline payload construction"
    - path: "backend/src/routes/AlertEvidenceReplay.contract.test.js"
      provides: "Contract tests for replay ordering, fallback behavior, and not_found handling"
  key_links:
    - from: "backend/src/routes/alerts.js"
      to: "backend/src/models/Alert.js"
      via: "Consume deterministic evidence sequence and window metadata for replay timeline"
    - from: "backend/src/routes/alerts.js"
      to: "backend/src/routes/AlertExplainability.contract.test.js"
      via: "Shared explainability/replay normalization consistency"
---

<objective>
Implement a replay interface endpoint that reconstructs suspicious movement progression from deterministic alert evidence in timeline order.

Purpose: satisfy EXP-04 with a stable investigator-facing replay payload.
Output: additive replay endpoint + contract tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-explainability-interface-and-evidence-replay/04-CONTEXT.md
@backend/src/routes/alerts.js
@backend/src/models/Alert.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED replay contract tests</name>
  <files>backend/src/routes/AlertEvidenceReplay.contract.test.js</files>
  <behavior>
    - Replay contract returns timeline/storyline ordered by timestamp then stable fallback key.
    - Replay response includes step index, transaction identifier, edge endpoints, and involved account context.
    - Replay endpoint preserves `not_found` behavior for missing alerts.
  </behavior>
  <action>Create failing tests for `GET /api/alerts/:id/evidence-replay` covering deterministic ordering, fallback when sequence is sparse, and missing alert contracts.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/routes/AlertEvidenceReplay.contract.test.js</automated>
  </verify>
  <done>Replay contract tests fail initially and encode EXP-04 invariants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement evidence replay endpoint and timeline normalization</name>
  <files>backend/src/routes/alerts.js, backend/src/routes/AlertEvidenceReplay.contract.test.js</files>
  <behavior>
    - Replay endpoint is additive and deterministic.
    - Timeline output contains normalized step metadata and sequence provenance.
    - Endpoint can synthesize minimal replay steps from transaction IDs if sequence edges are absent.
  </behavior>
  <action>Add `GET /:id/evidence-replay` route to alerts router. Build deterministic ordered timeline from normalized evidence sequence with account/transaction context and fallback synthesis from `transaction_ids`.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/routes/AlertEvidenceReplay.contract.test.js src/routes/AlertExplainability.contract.test.js</automated>
  </verify>
  <done>Replay endpoint meets ordering and reconstruction contract expectations for investigators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted evidence -> replay timeline API | Inconsistent ordering or sparse evidence can produce misleading progression narratives |
| investigator client -> replay endpoint | Clients may rely on replay ordering for compliance decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | I | replay timeline ordering | mitigate | Sort by parsed timestamp with deterministic tie-breakers and explicit index assignment |
| T-04-06 | T | sparse sequence fallback | mitigate | Synthesize clearly marked fallback replay steps from transaction_ids when edges missing |
| T-04-07 | R | replay provenance | mitigate | Return source metadata (`sequence` vs `transaction_ids`) per step for audit traceability |
| T-04-08 | D | endpoint stability | mitigate | Contract tests for not_found and deterministic output shape |
</threat_model>

<verification>
- `cd backend && npx jest --runInBand src/routes/AlertEvidenceReplay.contract.test.js src/routes/AlertExplainability.contract.test.js`
</verification>

<success_criteria>
- EXP-04 investigator replay timeline is available and deterministic.
- Timeline contains sequence/edge/account context for event reconstruction.
- Replay contracts are test-covered including sparse-evidence fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/04-explainability-interface-and-evidence-replay/04-02-SUMMARY.md`
</output>
