---
phase: 05-investigation-workflow-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/Case.js
  - backend/src/routes/cases.js
  - backend/src/routes/CaseWorkflow.contract.test.js
autonomous: true
requirements:
  - WFL-01
must_haves:
  truths:
    - "Investigator can assign or claim case ownership and SLA timers start automatically at first ownership action (WFL-01)."
    - "Escalation state is derived deterministically from SLA timing and exposed in case payloads (WFL-01)."
  artifacts:
    - path: "backend/src/models/Case.js"
      provides: "Case schema fields for SLA timing and escalation state"
    - path: "backend/src/routes/cases.js"
      provides: "Assignment/claim APIs and deterministic SLA escalation computation"
    - path: "backend/src/routes/CaseWorkflow.contract.test.js"
      provides: "Contract tests for ownership assignment/claim and SLA escalation behavior"
  key_links:
    - from: "backend/src/routes/cases.js"
      to: "backend/src/models/Case.js"
      via: "Persist and return assignment + SLA escalation fields"
---

<objective>
Implement ownership assignment/claim workflow with automatic SLA timers and deterministic escalation state updates for investigation operations.

Purpose: satisfy WFL-01 foundation before dashboard aggregation.
Output: case schema + routes + tests for assign/claim/SLA behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-investigation-workflow-dashboard/05-CONTEXT.md
@backend/src/models/Case.js
@backend/src/routes/cases.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED contract tests for ownership claim/assignment and SLA escalation fields</name>
  <files>backend/src/routes/CaseWorkflow.contract.test.js</files>
  <behavior>
    - Claim endpoint sets `assigned_to` to requesting user and initializes SLA timer fields.
    - Assignment endpoint sets assignee and initializes SLA timer fields.
    - Escalation state returns deterministic values based on due time thresholds.
  </behavior>
  <action>Create failing tests that lock payload shape and expected SLA/escalation transitions.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/routes/CaseWorkflow.contract.test.js</automated>
  </verify>
  <done>Tests fail initially with clear WFL-01 expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement schema + route ownership workflow behavior</name>
  <files>backend/src/models/Case.js, backend/src/routes/cases.js, backend/src/routes/CaseWorkflow.contract.test.js</files>
  <behavior>
    - Add case fields for `sla_started_at`, `sla_due_at`, and `escalation_state`.
    - Add `POST /:id/claim` and `PATCH /:id/assignment` endpoints.
    - Compute and persist escalation status (`ON_TRACK`, `AT_RISK`, `BREACHED`) during ownership actions and case reads.
  </behavior>
  <action>Implement deterministic SLA helpers in cases routes and persist fields through case model updates.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/routes/CaseWorkflow.contract.test.js</automated>
  </verify>
  <done>Ownership and SLA behavior pass contract tests.</done>
</task>

</tasks>

<verification>
- cd backend && npx jest --runInBand src/routes/CaseWorkflow.contract.test.js
- node .github/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/05-investigation-workflow-dashboard/05-01-PLAN.md --raw
</verification>

<success_criteria>
- WFL-01 ownership assignment/claim starts SLA timing automatically.
- Escalation state is deterministic and exposed for workflow consumers.
</success_criteria>

<quality_check>
Plan checker status: PASS (structure and references validated via `gsd-tools verify`).
</quality_check>

<output>
After completion, create .planning/phases/05-investigation-workflow-dashboard/05-01-SUMMARY.md
</output>
