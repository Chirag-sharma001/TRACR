---
phase: 05-investigation-workflow-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/cases.js
  - backend/src/routes/CaseWorkflow.contract.test.js
autonomous: true
requirements:
  - WFL-02
  - WFL-03
must_haves:
  truths:
    - "Case progression to no-file closure requires documented rationale and enforces explicit validation (WFL-02)."
    - "Manager can access oversight dashboard with backlog and timeliness/escalation summary for triage (WFL-03)."
  artifacts:
    - path: "backend/src/routes/cases.js"
      provides: "No-file rationale enforcement and manager-only oversight dashboard route"
    - path: "backend/src/routes/CaseWorkflow.contract.test.js"
      provides: "Contract tests for no-file validation and manager oversight payload"
  key_links:
    - from: "backend/src/routes/cases.js"
      to: "backend/src/auth/RBACMiddleware.js"
      via: "Role-gated oversight access aligned to existing role policy patterns"
---

<objective>
Complete investigation workflow safeguards by enforcing no-file rationale and exposing manager oversight triage metrics.

Purpose: satisfy WFL-02 and WFL-03 using additive backend contracts.
Output: route-level enforcement + manager dashboard + contract tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-investigation-workflow-dashboard/05-CONTEXT.md
@backend/src/routes/cases.js
@backend/src/auth/RBACMiddleware.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED contract tests for no-file rationale and oversight dashboard</name>
  <files>backend/src/routes/CaseWorkflow.contract.test.js</files>
  <behavior>
    - `CLOSED_DISMISSED` transition fails without `no_file_rationale`.
    - Manager oversight endpoint returns backlog/escalation/timeliness metrics and triage rows.
    - Non-manager role cannot access oversight endpoint.
  </behavior>
  <action>Create failing tests covering validation and role-gated dashboard behavior.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/routes/CaseWorkflow.contract.test.js</automated>
  </verify>
  <done>Tests fail initially and encode WFL-02/WFL-03 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement route enforcement and oversight aggregation</name>
  <files>backend/src/routes/cases.js, backend/src/routes/CaseWorkflow.contract.test.js</files>
  <behavior>
    - Enforce non-empty `no_file_rationale` on no-file closure transitions.
    - Persist rationale and include in transition audit metadata.
    - Expose manager dashboard aggregation of total/open/escalation/unassigned/backlog age signals and case rows.
  </behavior>
  <action>Implement validation + aggregation helpers and ensure deterministic ordering of dashboard triage rows.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/routes/CaseWorkflow.contract.test.js</automated>
  </verify>
  <done>All dashboard and no-file route contracts pass.</done>
</task>

</tasks>

<verification>
- cd backend && npx jest --runInBand src/routes/CaseWorkflow.contract.test.js
- node .github/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/05-investigation-workflow-dashboard/05-02-PLAN.md --raw
</verification>

<success_criteria>
- WFL-02 no-file closures require explicit rationale.
- WFL-03 manager dashboard exposes operational triage metrics.
</success_criteria>

<quality_check>
Plan checker status: PASS (structure and references validated via `gsd-tools verify`).
</quality_check>

<output>
After completion, create .planning/phases/05-investigation-workflow-dashboard/05-02-SUMMARY.md
</output>
