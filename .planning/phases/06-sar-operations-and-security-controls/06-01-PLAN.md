---
phase: 06-sar-operations-and-security-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/Case.js
  - backend/src/routes/cases.js
  - backend/src/sar/SARService.js
  - backend/src/routes/CaseWorkflow.contract.test.js
autonomous: true
requirements:
  - SAR-01
  - SAR-02
  - SAR-03
must_haves:
  truths:
    - "Investigator can generate SAR drafts from case-linked alert evidence with persisted case linkage (SAR-01)."
    - "Compliance can view deterministic SAR timeliness windows for upcoming, at-risk, and breached items (SAR-02)."
    - "Investigator can run deterministic SAR narrative quality/completeness checks before filing decisions (SAR-03)."
  artifacts:
    - path: "backend/src/routes/cases.js"
      provides: "Case-level SAR draft, quality-check, and deadline endpoints"
    - path: "backend/src/sar/SARService.js"
      provides: "Deterministic SAR narrative validation helper"
    - path: "backend/src/models/Case.js"
      provides: "SAR deadline field for explicit timeliness tracking"
    - path: "backend/src/routes/CaseWorkflow.contract.test.js"
      provides: "Contract coverage for SAR operations and deadline dashboard"
  key_links:
    - from: "backend/src/routes/cases.js"
      to: "backend/src/sar/SARService.js"
      via: "Generate and validate SAR drafts from linked case evidence"
---

<objective>
Implement SAR operations contracts for draft generation, deadline monitoring, and narrative quality checks.

Purpose: satisfy SAR-01/02/03 with deterministic backend contracts.
Output: case SAR endpoints + service validation + contract tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-sar-operations-and-security-controls/06-CONTEXT.md
@backend/src/routes/cases.js
@backend/src/sar/SARService.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED contract tests for case SAR operations and deadline dashboard</name>
  <files>backend/src/routes/CaseWorkflow.contract.test.js</files>
  <behavior>
    - `POST /:id/sar/draft` generates draft from case-linked alert and persists case `sar_draft_id`.
    - `POST /:id/sar/quality-check` returns deterministic quality score/issues/ready-to-file.
    - `GET /sar/deadlines` returns upcoming/at-risk/breached summary with triage rows.
  </behavior>
  <action>Create failing contract tests encoding SAR-01/02/03 API behavior.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/routes/CaseWorkflow.contract.test.js</automated>
  </verify>
  <done>Tests fail initially with clear SAR operations expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement case-level SAR operations and service-level quality validation</name>
  <files>backend/src/models/Case.js, backend/src/routes/cases.js, backend/src/sar/SARService.js, backend/src/routes/CaseWorkflow.contract.test.js</files>
  <behavior>
    - Add case deadline field and deterministic deadline status helpers.
    - Add case-level SAR draft generation endpoint with traceable linkage.
    - Add service validator for narrative quality/completeness and route endpoint for pre-filing checks.
  </behavior>
  <action>Implement additive endpoints and deterministic validators with audit events for sensitive operations.</action>
  <verify>
    <automated>cd backend && npx jest --runInBand src/routes/CaseWorkflow.contract.test.js src/sar/SAR.property.test.js</automated>
  </verify>
  <done>SAR operation and quality contracts pass.</done>
</task>

</tasks>

<verification>
- cd backend && npx jest --runInBand src/routes/CaseWorkflow.contract.test.js src/sar/SAR.property.test.js
- node .github/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/06-sar-operations-and-security-controls/06-01-PLAN.md --raw
</verification>

<success_criteria>
- SAR-01 draft generation is case-linked and evidence-grounded.
- SAR-02 timeliness dashboard surfaces operational deadline risk.
- SAR-03 pre-filing quality checks are deterministic and machine-readable.
</success_criteria>

<quality_check>
Plan checker status: PASS (structure and references validated via `gsd-tools verify`).
</quality_check>

<output>
After completion, create .planning/phases/06-sar-operations-and-security-controls/06-01-SUMMARY.md
</output>
